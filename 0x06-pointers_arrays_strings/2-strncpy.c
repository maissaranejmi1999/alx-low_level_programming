#include "main.h"

/**
 * _strncpy - concatenates
 * @dest: string
 * @src: string
 * @n: bytes
 *
 * Return: string concatenated
 */


char *_strncpy(char *dest, char *src, int n)
{
	int i = 0;

	for (i = 0; i < n; i++)
	{
		dest[i] = src[i]
	}
	dest[i] = '\0';
	return (0);
}
