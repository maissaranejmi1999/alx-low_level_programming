#include "main.h"

/**
 * _isdigit - function
 *
 * Description: checks is c is uppercase letter
 *
 * @c: is a variable
 *
 * Return: 1 if upper
*/

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	} else
		return (0);
	return (0);
}
